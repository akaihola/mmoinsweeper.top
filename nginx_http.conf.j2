server {
    listen 80;
    server_name {{ domain_name }};

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        root /opt/mmoinsweeper/public;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
